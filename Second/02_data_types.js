// two types of data types : primitive and non-primitive(reference) data type

// primitve data types:

let a = 10;           // an integer
let b = 12.45;        // a floating point
let c = "JavaScript"; // a string
let d = true;         // a boolean value
let e = false;        // a boolean value
let f = null;         // a null value
let g = undefined;    // an undefined value
let h = "";           // an empty string
let i;                // an undefined value

console.log(a); // console.log() function is used to print or display anything while running the code

// above all are valid data types

// non-primitive data types:

// object, array, regexp